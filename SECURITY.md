# 安全说明

## 威胁模型

Hephaestus 是**单用户**平台：它假定登录者本人可信，并且默认只监听 `127.0.0.1:9016`。它不是多租户系统，Project 是工作上下文与文件组织边界，**不是**用户之间的安全隔离边界。

在这个前提下，认证要防的是「本机之外的人访问这个服务」，而不是「已登录用户越权」。已登录用户在设计上就能使用被启用的全部能力，包括文件读写与 Shell。

## 登录与凭据

`HEPHAESTUS_AUTH_PASSWORD` **以明文保存在 `.env` 中**，这是有意为之，原因如下：

浏览器登录不直接发送密码，而是提交 `SHA-256(password + timestamp + salt)`。服务端必须用同样的输入重算这个摘要才能校验，因此它需要密码原文，无法只保存密码哈希。

之所以采用这种挑战摘要而不是「明文密码走 TLS」，是因为本项目的默认形态是本机 HTTP 访问：在非 HTTPS 源上浏览器不提供 `crypto.subtle`，所以前端内置了一份 SHA-256 实现来计算摘要。

相应的限制与保护：

- `.env` 应设置为仅属主可读（`chmod 600 .env`）。它已在 `.gitignore` 中。
- 摘要包含时间戳，只在前后 5 分钟窗口内有效，且同一组 `(用户名, 时间戳, salt, 摘要)` 不能重复使用。
- 连续 5 次失败后，登录需要先完成一次 18 位前导零的工作量证明；该计数是**全局**的，不区分来源 IP。
- 会话 Cookie 为 HttpOnly、SameSite=Strict，有效期 14 天，剩余不足 7 天时自动续期。
- `HEPHAESTUS_JWT_SECRET` 至少 32 字节，且不要复用登录密码。

这套机制**不能替代 TLS**。摘要在链路上被截获仍可在 5 分钟窗口内重放一次。

## 部署要求

- 不要把监听端口直接暴露到互联网。默认绑定回环地址；远程访问请使用 SSH 端口转发，或置于终止 TLS 的反向代理之后。
- 在反向代理后部署时，必须设置 `HEPHAESTUS_COOKIE_SECURE=true`。否则 Hephaestus 看到的是明文 HTTP 请求，会话 Cookie 不会带上 `Secure` 属性。
- 监听非回环地址且未设置该变量时，启动日志会给出警告。

## 能力开关不是安全边界

以下开关是给单个可信用户的便利设置，不构成防护：

- `HEPHAESTUS_SHELL_ENABLED=true` 允许模型执行 Shell 命令。内置的高危命令模式匹配（`sudo`、`shutdown`、fork 炸弹、管道到 shell 等）只拦截常见误操作，可以被轻易绕过（例如改写命令或通过 `python -c`），它的作用是触发人工确认，而不是阻断攻击。
- `HEPHAESTUS_PROJECT_ACCESS_OVERRIDE=true` 取消文件工具对 Project 目录与系统临时目录的限制，使其可访问任意路径。
- `/interact auto-approve` 的自动授权仅存在于当前服务进程内，重启后失效。

启用 Shell 或路径覆盖，等同于把执行该进程的操作系统账户的全部权限交给模型。请结合部署环境谨慎评估。

## 已实现的防护

- `web_fetch` 拒绝 `http`/`https` 之外的协议，并在主机名与解析出的 IP 两个层面拒绝私有地址与回环地址，本地无头浏览器同样经由校验代理出网。
- 上传有数量、单文件与总量上限，请求体经 `MaxBytesReader` 限制。
- 聊天历史检索对 LIKE 元字符做转义，查询通过参数化语句下发。
- 除登录与健康检查外的全部 API（含 Swagger UI）都要求认证。

## 报告问题

发现安全问题请通过仓库的 private vulnerability reporting 提交，或直接联系维护者，不要公开 issue。请附上复现步骤与影响范围；在修复发布前请勿披露。
